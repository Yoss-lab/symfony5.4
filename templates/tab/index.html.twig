<!doctype html>
<html lang"fr">
<head>
    <title></title>
    <link rel="stylesheet" href="{{ asset('assets/node_modules/bootstrap/dist/css/bootstrap.css')}}">

</head>
<body>
<table class="table">
{% for note in notes %}
    <tr>
        <td>{{note}}</td>
    </tr>
{% endfor %}

</table>
<div class="alert alert-info"> le nombre des eleves {{ notes | length }} </div>
<div class="alert alert-info"> la note maximale {{ max(notes) }} </div>
<div class="alert alert-info"> la note minimale {{ min(notes) }} </div>
</body>
</html>